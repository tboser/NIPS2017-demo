/* this is C++ */
#include <string>
#include <vector>
struct Layer {
  std::string name;
  std::array<size_t, 4> weights_shape;
  std::vector<int8_t> weights;
  size_t bias_shape;
  std::vector<int8_t> biases;
};
